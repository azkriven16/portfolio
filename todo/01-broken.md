# 01 — Broken

- [x] **B1** `/projects` returned 500 in dev (`<Link><a>`). Rebuilt in the site's style, with metadata. Verified 200.
- [x] **B2** Resume added by Euger as `public/euger_bonete_resume_dev.pdf`. Navbar (mobile + desktop) and ⌘K palette now point at it and open in a new tab. Verified 200 `application/pdf`.
- [x] **B3** Email changed from `eugerbone@email.com` to `eugerbone@gmail.com` (confirmed by Euger) in `Bio.tsx`, `Contact.tsx`, `CommandPalette.tsx`.
- [x] **B4** No dead links left in `Bio.tsx`. BSIT and Rocketshyft are plain text; Digipay links to https://digipay.ph/.
- [x] **B5** No visible Blog/Guestbook links at 768–1023px. First fix (four flat links) crowded the navbar at every width. Replaced with a shadcn-style Navigation Menu: **Work** (Projects, Experience, Contact) and **Explore** (Blog, Guestbook), opening on hover, click or keyboard. Verified in a real browser at 1905px and 800px, dark and light.
- [x] **B6** Blog rendered Markdown literally. Added `InlineMarkdown` (code spans + links). Verified `useChat` now renders as `<code>`.
- [x] **B7** (found during fixes) Light theme had `--c-text-4: #bbbbb` (5-digit, invalid hex). Fixed to `#bbbbbb`.
- [x] **B8** (found 2026-09-24) Page navigations smooth-scrolled to the top instead of starting there, e.g. `/side-projects` → home animated from 649px down to 0 over about 400ms. Next 16 stopped overriding the global `scroll-behavior: smooth` during route changes. Added `data-scroll-behavior="smooth"` to `<html>` (the documented opt-in), so navigations jump to the top and in-page anchors (`/#contact`) still scroll smoothly. Verified in headless Chromium against a production build. Also looked into the React "Encountered a script tag" warning seen earlier: it only fires when a hydration mismatch forces the client to re-render the whole tree (a stale dev server did that). It doesn't reproduce on a fresh server, so the pre-paint theme script is left as is.
- [x] **B9** (found 2026-09-24, fixed 2026-09-25) **Vercel preview deployments failed** with `Resource provisioning failed` before building, on every PR from #22 to #33. Cause: the portfolio's Neon store (`neon-crimson-coin`, Vercel native integration) was connected to Preview too, which adds a *required* per-deploy step that creates a Neon branch for each new git branch. New branches failed (almost certainly the free plan's branch limit), and the one that worked (#19) reused an existing branch. Cleaning up in the Neon console wasn't possible: the Vercel→Neon account-link email looped ("already verified"). **Fix (chosen by Euger):** reconnected the store with `vercel integration resource connect neon-crimson-coin portfolio -e production -e development --prefix DATABASE_`, so previews no longer provision a database. The same 16 `DATABASE_*` variables are back for Production/Development, and the new production credentials read the same 4 guestbook rows. **Trade-off:** preview deployments have no database, so `/guestbook` and the guestbook count fail on preview links only. The old `preview/*` Neon branches still exist (unused); delete them if the Neon console login ever works.
