# 02 — Not real yet

- [x] **N1** Guestbook now backed by Neon Postgres (`src/lib/db.ts`): server action (`src/app/guestbook/actions.ts`) inserts, page is a Server Component reading live entries, `FixedChrome` count fetches `/api/guestbook/count`. Table auto-created on first query. `DATABASE_URL` connected via Vercel's Neon integration (project `portfolio`, Singapore region, free tier); `vercel env pull` confirmed locally. Verified: `/guestbook` returns 200, `/api/guestbook/count` reflects live count, and a direct insert/read/delete round-tripped against the real DB. **Cleaned up 2026-09-24:** deleted the 8 made-up seed entries (Grace Kim … Ivy Martinez) and kept the 4 that look like real past visitors (NPC, danskyvich, samwan, Kuz). Removed the auto-seed code, which used to re-insert the whole placeholder list whenever the table was empty, and the placeholder array in `src/data/guestbook.ts`. A backup was taken before deleting but was lost with the session scratchpad. The deleted rows are only the made-up entries, which are still in git history (`src/data/guestbook.ts` before #29).
- [x] **N2** FixedChrome counts were hardcoded. The blog count comes from `posts`, and the guestbook count from `/api/guestbook/count` (live DB).
- [x] **N3** README corrected: removed NeonDB/Drizzle/MDX claims and documented `NEXT_PUBLIC_BASE_URL`, `DATABASE_URL` and `RESEND_API_KEY`. Intro and feature list brought up to date on 2026-09-25 (guestbook, contact form, contributions chart, Shiki, project filters, `/side-projects`, `/uses`, OG images).
- [x] **N4** The Daily Activity workflow never ran: GitHub rejected the file (`Unexpected symbol: '%'`, since Actions expressions have no modulo). Fixed in #13: the modulo check moved into a shell step, the stats job uses `GITHUB_TOKEN` and fails on bad API data instead of writing `null`, and the ping job `needs` the stats job. Follow-up (#21): the ping job then failed 3 of 4 runs with `! [rejected] main -> main (fetch first)`, because its checkout is the trigger SHA and was behind the stats commit just pushed. Both jobs now `git pull --rebase origin main` before pushing. Verified with manual run #9, which hit the exact race and pushed cleanly.
- [x] **N5** Experience bullets for Viafide / Global Talent Portal / Ciptax rewritten to match the project pages (approved by Euger). Bio "Skilled in" badges now include Tailwind CSS, Supabase and Cloudflare (approved by Euger 2026-09-24).
- [x] **N6** Every project now has a homepage screenshot (`public/projects/<slug>.jpg`, 1200×630 at 2x, JPEG q85, 140–520 KB), shown on its `/projects/<slug>` page through `next/image`. Captured 2026-09-24 from the live sites with headless Chromium: dark color scheme, cookie banners declined, with Inter and Noto Color Emoji installed so system-font text and emoji render the way a real browser shows them. **To refresh:** screenshots go stale when the client sites change, so recapture them when that happens.
- [x] **N7** Decision: keep the activity-ping job (see N4). It adds commits like "chore: routine maintenance" to `.github/activity.log` on most scheduled runs.
