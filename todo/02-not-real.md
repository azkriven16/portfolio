# 02 — Not real yet

- [x] **N1** Guestbook now backed by Neon Postgres (`src/lib/db.ts`): server action (`src/app/guestbook/actions.ts`) inserts, page is a Server Component reading live entries, `FixedChrome` count fetches `/api/guestbook/count`. Table auto-created and seeded from the old placeholder list on first query. `DATABASE_URL` connected via Vercel's Neon integration (project `portfolio`, Singapore region, free tier); `vercel env pull` confirmed locally. Verified: `/guestbook` returns 200, `/api/guestbook/count` reflects live count, and a direct insert/read/delete round-tripped against the real DB. **Still true:** the seeded entries are the same placeholder-looking names as before — they're now real persisted rows, but you may want to clear them out via the Neon console so the guestbook starts empty/authentic rather than pre-filled.
- [x] **N2** FixedChrome counts were hardcoded; now derived from `posts` and `guestbookEntries`.
- [x] **N3** README corrected: removed NeonDB/Drizzle/MDX claims, documented `NEXT_PUBLIC_BASE_URL`, guestbook described as client-side only. Since N1, the README describes the Neon backend; only the intro line still says "a guestbook UI".
- [~] **N4** Root cause: GitHub rejected the whole workflow file (`Unexpected symbol: '%'` at line 63 — Actions expressions have no modulo), so neither job had ever run. **Fixed locally, not yet pushed**: modulo check moved into a shell step; stats job now uses `GITHUB_TOKEN` and fails on bad API data instead of writing `null`; ping job `needs` the stats job so the two never push at once. `actionlint` is clean and the stats script was run against the live API (valid JSON). Pushed and running: stats commits land daily. **New issue (2026-09-24):** the activity-ping job failed 3 of 4 scheduled runs on 2026-09-23 with `! [rejected] main -> main (fetch first)`: its checkout is the trigger SHA, so it was behind the stats commit job 1 had just pushed. Fixed on `fix/activity-push-race` (`git pull --rebase origin main` before each push). **Remaining:** merge, then confirm the next scheduled run is green.
- [x] **N5** Experience bullets for Viafide / Global Talent Portal / Ciptax rewritten to match the project pages (approved by Euger). Bio "Skilled in" badges now include Tailwind CSS, Supabase and Cloudflare (approved by Euger 2026-09-24).
- [?] **N6** No project has a screenshot. `image` is supported but never set. Needs images (and a remote pattern in `next.config.ts` if hosted elsewhere).
- [x] **N7** Decision: keep the activity-ping job. Made it valid (see N4). Note it now runs for the first time and will add commits like "fix: correct edge case" to `.github/activity.log`.
